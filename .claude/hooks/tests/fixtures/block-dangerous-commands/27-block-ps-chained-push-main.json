{
  "name": "block push to main when chained after a PowerShell Set-Location",
  "stdin": { "tool_input": { "command": "Set-Location C:\\Users\\unseensnick\\Desktop\\projects\\my-soffle; git push origin main" } },
  "expect_exit": 2,
  "expect_stdout_contains": ["deny", "main"]
}
