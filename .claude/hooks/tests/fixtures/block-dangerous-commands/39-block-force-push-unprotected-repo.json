{
  "name": "block force push even in a repo whose main is unprotected",
  "stdin": { "cwd": "C:\\Users\\me\\notes-repo", "tool_input": { "command": "git push --force origin main" } },
  "env": { "CLAUDE_UNPROTECTED_REPOS": "notes-repo" },
  "expect_exit": 2,
  "expect_stdout_contains": ["deny", "force push"]
}
