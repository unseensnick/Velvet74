{
  "name": "block reading credentials.json with the gc alias",
  "stdin": { "tool_input": { "command": "gc config\\credentials.json" } },
  "expect_exit": 2,
  "expect_stdout_contains": ["deny", "secret file"]
}
