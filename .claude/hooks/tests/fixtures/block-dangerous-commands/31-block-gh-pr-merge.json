{
  "name": "block gh pr merge (merging is the owner's call)",
  "stdin": { "tool_input": { "command": "gh pr merge 12 --merge --repo unseensnick/my-soffle" } },
  "expect_exit": 2,
  "expect_stdout_contains": ["deny", "owner's call"]
}
